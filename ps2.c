#include <stdio.h>
#include <limits.h>
#include <float.h>


int main(){

    printf("Size of char is %zu\n", sizeof(char));
    printf("Minimum value of char is %d\n", CHAR_MIN);
    printf("Maximum value of char is %d\n", CHAR_MAX);
    

    return 0;
}